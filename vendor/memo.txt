      // ob_start();
            // include "www/admin/manageForm/displayDetailForm.phtml";
            // $content = ob_get_clean();

            // $html2pdf = new Html2Pdf('P', 'A4', 'fr');
            // $html2pdf->setDefaultFont("Arial");
            // $html2pdf->writeHTML($content);

            // $html2pdf->Output("detail-form.pdf");